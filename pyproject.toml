[project]
name = "internship"
version = "0.1.0"
description = "A small Python project for practicing setup, dependency management, and concurrency benchmarks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]
